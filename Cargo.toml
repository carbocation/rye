[package]
name = "rye-core"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rng-compat-r = "=0.1.0"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
